总结：
离散化：1-1e9 没法存储。但是只会给5000个区间，意味着最多10000个数。
假设这10000个数中，每个数都会有自己在里面的序号，比如1000000可能是第2大的数。
我们将数存储进num[10000]数组，再将它排序，去重，就找到了原来数现在对应的位置，他可缩小到的大小。

细节：edge[i].s-- ，通过改变区间的含义，使得题目简化。这样操作后，一个区间[s, e]就变成了，(s, e]

带权并查。





